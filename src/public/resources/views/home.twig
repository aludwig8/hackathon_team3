{% extends 'layouts/app.twig' %}

{% block content %}
	
	<h1>TEST:</h1>

	{% for item in article %}
        <p>{{ item.role_name }}</p>
    {% endfor %}


    <form action="{{ path_for('user.store') }}" method="post">

    <input type="submit" name="submit">
    </form>

{% endblock %}